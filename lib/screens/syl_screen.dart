import 'package:flutter/material.dart';

class SylScreen extends StatelessWidget {
  const SylScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Syl Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}